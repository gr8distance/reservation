<%= form_with(model: reservable_time) do |form| %>
  <% if reservable_time.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reservable_time.errors.count, "error") %> prohibited this reservable_time from being saved:</h2>

      <ul>
        <% reservable_time.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :minute, style: "display: block" %>
    <%= form.number_field :minute %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
