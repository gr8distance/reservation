<%= form_with(model: business_hour) do |form| %>
  <% if business_hour.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(business_hour.errors.count, "error") %> prohibited this business_hour from being saved:</h2>

      <ul>
        <% business_hour.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :start_at, style: "display: block" %>
    <%= form.datetime_field :start_at %>
  </div>

  <div>
    <%= form.label :end_at, style: "display: block" %>
    <%= form.datetime_field :end_at %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
